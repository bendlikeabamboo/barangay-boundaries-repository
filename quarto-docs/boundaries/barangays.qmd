---
title: Barangays
description: |
  Download all 42,026 Philippine barangay boundaries as a single GeoJSON file enriched with PSGC
  codes, derived from NAMRIA shapefiles and PSA PSGC snapshots.
author: Manuel Balmeo
format:
  html:
    toc: true
    code-copy: true
    lightbox: auto
---

Download **all Philippine barangay boundaries** as one GeoJSON file, every polygon tagged with its
canonical **PSGC code**. With 42,026 barangay polygons (snapshot `2023-10-24`), this is the
lowest-level administrative boundary dataset for the Philippines.

## Download

The barangay file is available through all three pipeline tiers. The **Hierarchical** tab is the
curated, recommended download; **Enriched** and **Raw** are intermediate pipeline stages documented
for traceability.

::: {.panel-tabset}

## Hierarchical (curated / recommended)

Per-class extract — barangays only:

```bash
curl -LO https://github.com/bendlikeabamboo/barangay-boundaries-repository/releases/download/v2026.4.13.0/barangays.geojson
```

## Enriched (pipeline stage)

ADM4 file with PSGC codes (in-repo only):

```bash
curl -LO https://raw.githubusercontent.com/bendlikeabamboo/barangay-boundaries-repository/main/2023-10-24/enriched_t0p005/adm4.geojson
```

## Raw (pipeline stage)

NAMRIA-converted, pre-enrichment (in-repo):

```bash
curl -LO https://raw.githubusercontent.com/bendlikeabamboo/barangay-boundaries-repository/main/2023-10-24/raw_t0p005/adm4.geojson
```

:::

The enriched and raw tabs above are intermediate pipeline stages; see
[Releases & versioning](download.qmd) for the tag convention and the in-repo stage paths.

## Coverage (ADM4)

| Metric           | Value |
|------------------|------:|
| PSGC barangays   | 42,010 |
| NAMRIA polygons  | 41,825 |
| Matched          | 41,803 |
| Coverage         | 99.947% |
| Exact matches    | 38,771 |
| Fuzzy matches    | 3,255 |

## Properties per feature

Each barangay feature includes `psgc_code` (10-digit), `psgc_name`, `psgc_status`,
`match_confidence`, plus the original NAMRIA attributes (e.g. `ADM4_EN`, `ADM4_PCODE`).

## Use it

Load into geopandas, QGIS, or any GeoJSON reader, then join on `psgc_code` to census, health,
election, or financial data keyed by Philippine Standard Geographic Code.

## Limitations

::: {.callout-warning}
Coverage gaps in this barangay layer:

- ~198 PSGC barangays (mostly in BARMM and Metro Manila) — and ~230 PSGC entities in total — have
  no NAMRIA polygon.

- 6 features could not be resolved to a PSGC entity (`unresolved.geojson`).
:::
